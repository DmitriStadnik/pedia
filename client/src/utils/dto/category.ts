export type Category = {
  slug: string;
  title: string;
  _id: string;
};

export type CategoryUpdateDTO = {
  slug: string;
  title: string;
  _id: string;
};

export type CategoryCreateDTO = {
  slug: string;
  title: string;
};
